import React, { memo } from 'react'
import { logRoles } from '@testing-library/react'

export default memo(function JKDiscover() {
    return (
        <div>
            JKDiscover
        </div>
    )
})
